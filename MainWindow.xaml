<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TripleDevBox.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TripleDevBox"
    xmlns:controls="using:TripleDevBox.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          Padding="12"
          RowSpacing="12">
        <Grid.RowDefinitions>
            <!-- Top row: sizes to the two tools' natural height -->
            <RowDefinition Height="Auto" />
            <!-- Bottom row: JSON tool fills all remaining space -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top area: timestamp (left) + GUID (right), side by side -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:TimestampConverter Grid.Column="0" />
            <controls:GuidGenerator Grid.Column="1" />
        </Grid>

        <!-- Bottom area: JSON escape / unescape, full width, fills the rest -->
        <controls:JsonEscaper Grid.Row="1" />
    </Grid>
</Window>
